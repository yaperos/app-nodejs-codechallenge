import { SwaggerConfig } from '../../interfaces/swagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'Service Transactions API',
  description: 'Transactions API description',
  version: '1.0',
  tags: ['API'],
};
