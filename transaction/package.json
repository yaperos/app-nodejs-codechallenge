{
  "name": "anti-fraud",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:debug": "nest start --debug --watch",
    "start:prod": "node dist/main",
    "build": "nest build",
    "test": "jest",
    "lint": "eslint .",
    "prettify": "prettier --write \"src/**/*.ts\"",
    "format": "npm run prettify",
    "watch": "nest start --watch",
    "migration": "npx prisma migrate dev --name init",
    "start:migrate:prod": "npx prisma migrate deploy --schema ./prisma/schema.prisma && npm run start:prod"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nestjs/cli": "^10.1.7",
    "@nestjs/testing": "^10.0.5",
    "@types/express": "^4.17.17",
    "@types/jest": "^29.5.3",
    "prisma": "^5.0.0",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/mapped-types": "^2.0.2",
    "@nestjs/microservices": "^10.0.5",
    "@nestjs/platform-express": "^10.0.0",
    "@prisma/client": "^5.0.0",
    "dotenv": "^16.3.1",
    "kafkajs": "^2.2.4",
    "reflect-metadata": "^0.1.13"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  }
}
